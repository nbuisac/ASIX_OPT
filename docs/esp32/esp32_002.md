# Classes i mètodes

Per treballar amb la placa *ESP-32* i *ESP32-S3* caldrà utilitzar algunes llibreries que cal importar abans:

## `class machine`

Depenent del que necessitem podem importar tota la llibreria o només alguns dels seu components

=== "TOT"

    ```py
    import machine
    ```

=== "Pin"

    ```py
    from machine import Pin
    ```


* `machine.freq(freq_val)`: quan no s'especifica `freq_val`, torna la freqüència actual de la CPU; en cas contrari, és per establir la freqüència actual de la CPU.

    `freq_val`: 80000000 (80MHz)、160000000(160MHz)、240000000(240MHz)

* `machine.reset()`: una funció de reinici. Quan es crida, el programa es reiniciarà.

* `machine.unique_id()`: Obté l'adreça MAC del dispositiu.

* `machine.idle()`: desactiva qualsevol funció temporalment no utilitzada del xip i del seu rellotge, cosa que és útil per reduir el consum d'energia en qualsevol moment durant períodes curts o llargs.

* `machine.disable_irq()`: desactiva les sol·licituds d'interrupció i retorna l'estat d'*IRQ* anterior. La funció  `disable_irq ()` i la funció `enable_irq()` s'han d'utilitzar juntes, en cas contrari, la màquina *"petarà"* i reiniciarà.

* `machine.enable_irq(state)`: per tornar a habilitar les sol·licituds d'interrupció. L'estat del paràmetre hauria de ser el valor que s'ha retornat des de la crida més recent a la funció `disable_irq()`.

* `machine.time_pulse_us(pin, pols_level, timeout_us=1000000)`: Comprova la durada del nivell de pols extern al pin donat i retorna la durada del nivell de pols extern en microsegons. Quan el nivell de pols = 1, prova la durada del nivell alt; Quan el nivell de pols = 0, això prova la durada del nivell baix.

    Si el nivell de configuració no és coherent amb el nivell de pols actual, s'esperarà fins que siguin coherents i llavors comença a cronometrar. Si el nivell establert és coherent amb el nivell de pols actual, començarà a cronometrar immediatament.

    Quan el nivell de pin és oposat al nivell establert, esperarà el temps d'espera i tornarà "-2". Quan el pin nivell i el nivell establert és el mateix, també esperarà el temps d'espera però tornarà "-1". timeout_us és la durada de temps d'espera.

## `class Pin`

Abans d'utilitzar el mòdul `Pin`, cal importar-lo de `machine` o bé referir-nos a ell havent importat `machine` i fent referència a aquesta llibreria

=== "Treballem amb `Pin` directament"

    ```py
    from machine import Pin
    
    led = Pin(2, Pin.OUT)
    ```
    
=== "importem `machine`i utilitzem `Pin`"

    ```py
    import machine
    
    led = machine.Pin(2, machine.Pin.OUT)
    ```

* `Class Pin(id[, mode, pull, valor])`:
    
    * `id`: número de pin arbitrari

    * `mode`: Mode de pins

        * `Pin.IN`: Mode d'entrada

        * `Pin.OUT`: Mode de sortida

        * `Pin.OPEN_DRAIN`: mode de drenatge obert

    * `Pull`: si s'ha d'activar el mode intern d'amunt i avall
        
        * `Cap`: no hi ha resistències de tirar cap amunt o cap avall
        
        * `Pin.PULL_UP`: Mode pull-up, que genera un alt nivell per defecte
        
        * `Pin.PULL_DOWN`: Mode desplegable, que genera un nivell baix per defecte

    * `valor`: Estat del nivell del pin, 0/1

* `Pin.init(mode, pull)`: inicialitza els pins

* `Pin.value([valor])`: obteniu o definiu l'estat del nivell de pins, retorneu 0 o 1 segons el nivell lògic dels pins.

    * Sense paràmetre, llegeix el nivell d'entrada. Amb el paràmetre donat, és establir el nivell de sortida.

    * `valor`: pot ser vertader/fals o 1/0.

* `Pin.irq(trigger, handler)`: configura un controlador d'interrupcions per ser cridat quan el nivell de pin compleix una condició.

    * `trigger`:

        * Pin.IRQ_FALLING: interrupció en el front de baixada

        * Pin.IRQ_RISING: interrupció en el front ascendent

        * 3: interrupció a les dues vores
    
    * `handler`: funció de devolució de trucada

## `class time`

Utilitzarem aquesta classe per esperar un temps o per a saber quant temps ha passat entre diferents moments.

Cal importar aquest mòdul abans d'utilitzar-lo

```py
import time
```

* `time.sleep(sec)`: Dorm durant el nombre de segons donat

    * `sec`: aquest argument hauria de ser un `int` o un `float`.


* `time.sleep_ms(ms)`: dorm durant el nombre donat de mil·lisegons, `ms` hauria de ser un `int`.

* `time.sleep_us(us)`: dorm durant el nombre donat de microsegons, `us` hauria de ser un `int`.

* `time.time()`: Obté la marca de temps de la CPU, amb *segon* com a unitat.

* `time.ticks_ms()`: retorna el valor del *comptador de mil·lisegons* que augmenta, que es recompta després d'alguns valors.

* `time.ticks_us()`: retorna microsegons

* `time.ticks_cpu()`: semblant a `ticks_ms()` i `ticks_us()`, però és més precís (rellotge de retorn de la CPU).

* `time.ticks_add(ticks, delta)`: obté la marca de temps després del desplaçament.

    * `ticks`: `ticks_ms()`、`ticks_us()`、`ticks_cpu()`

    * `delta`: *delta* pot ser un nombre enter arbitrari o una expressió numèrica

* `time.ticks_diff(old_t, new_t)`: calcula l'interval entre dues marques de temps, com ara `ticks_ms()`, `ticks_us()` o `ticks_cpu()`.

    * `old_t`: hora d'inici

    * `new_t`: hora de finalització



[wokwiDevelop]: https://wokwi.com/projects/new/micropython-esp32
[wokwiSamples]: https://wokwi.com/micropython
[https://ledcalculator.net/es]: https://ledcalculator.net/es    "led calculator"
[Pin]:  https://docs.micropython.org/en/latest/library/machine.Pin.html

--8<-- ".acronims.txt"